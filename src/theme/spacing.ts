import { scaleFont } from '../utils/responsive';

export const spacing = {
  xs: scaleFont(4),
  sm: scaleFont(8),
  md: scaleFont(12),
  lg: scaleFont(16),
  xl: scaleFont(20),
  xxl: scaleFont(24),
};
