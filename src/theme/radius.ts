import { scaleFont } from '../utils/responsive';

export const radius = {
  sm: scaleFont(8),
  md: scaleFont(12),
  lg: scaleFont(16),
  xl: scaleFont(24),
  xxl: scaleFont(32),
  borderRadius: scaleFont(12),
  full: 9999,
};
