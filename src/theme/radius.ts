import { scaleFont } from '../utils/responsive';

export const radius = {
  sm: scaleFont(6),
  md: scaleFont(8),
  lg: scaleFont(10),
  xl: scaleFont(12),
  borderRadius: scaleFont(4),
  full: 9999,
};
